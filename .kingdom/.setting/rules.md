# rules — moved (v0.34.0)

Rules are now **one rule per file** under [`rules/`](rules/). Start at the registry:

➡ **[`rules/index.md`](rules/index.md)** — the Tier-1 legend + every rule (`R01`…`R52`) with its file.

Open only the rule files you need; the registry table links each `R##` to its file.

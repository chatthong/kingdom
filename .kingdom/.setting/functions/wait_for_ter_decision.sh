#!/usr/bin/env bash
# kingdom function: wait_for_ter_decision

wait_for_ter_decision () {
  # Interactive: the King pauses and waits for the user's next chat message. Only the literal
  # word "push" (or a close variant) authorises the push per R1. The wait happens in the King's
  # conversation loop, not in bash — this stub exists so callers resolve. See R01.
  : # see rules/R01-push-approval-is-single-shot.md
}
